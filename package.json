{
  "name": "maliyye-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp style.css dist/ && cp script.js dist/ && cp translations.json dist/",
    "lint": "echo 'Lint skipped' && exit 0",
    "test": "echo 'No tests yet' && exit 0"
  }
}
